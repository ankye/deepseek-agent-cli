# Tasks

- [x] 1. Add OpenSpec deltas for diagnostics environment preparation and local readiness profile evidence.
- [x] 2. Add failing CLI tests for `diagnostics env prepare` dry-run, JSONL dependency records, invalid positional arguments, and secret redaction.
- [x] 3. Implement diagnostics command parsing for `env prepare`, `--profile`, `--execute`, and dry-run behavior.
- [x] 4. Implement a built-in `swe-bench-lite` profile runner with deterministic dependency probes, allowlisted command plans, execution evidence, and redacted credentials.
- [x] 5. Wire text, JSON, and JSONL rendering into the existing diagnostics result pipeline.
- [x] 6. Validate OpenSpec and run focused/broader verification.

# 任务

- [x] 1. 为 diagnostics environment preparation 与 local readiness profile evidence 增加 OpenSpec delta。
- [x] 2. 为 `diagnostics env prepare` dry-run、JSONL dependency records、非法 positional arguments 与 secret redaction 增加先失败的 CLI 测试。
- [x] 3. 实现 `env prepare`、`--profile`、`--execute` 与 dry-run 行为的 diagnostics 命令解析。
- [x] 4. 实现内置 `swe-bench-lite` profile runner，包含确定性 dependency probe、allowlisted command plan、执行证据与 credential 脱敏。
- [x] 5. 接入现有 diagnostics result pipeline 的 text、JSON、JSONL 渲染。
- [x] 6. 校验 OpenSpec 并运行聚焦/扩大验证。
