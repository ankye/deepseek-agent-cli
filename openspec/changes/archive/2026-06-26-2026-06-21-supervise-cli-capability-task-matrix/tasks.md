# Tasks

## OpenSpec / 规格

- [x] Create this OpenSpec change for a supervised CLI capability task matrix.
- [x] Define supervisor boundaries: evaluator, not helper.
- [x] Define initial task matrix covering read-only, edit, test-first, search/context, permission, recovery, long-task, and artifact delivery capabilities.
- [x] Define result classifications and evidence artifacts.
- [x] Validate this change with `npx openspec validate 2026-06-21-supervise-cli-capability-task-matrix --strict`.
- [x] Validate canonical specs with `npx openspec validate --specs --strict`.

- [x] 创建 supervised CLI capability task matrix OpenSpec change。
- [x] 定义 supervisor 边界：evaluator，不是 helper。
- [x] 定义初始任务矩阵，覆盖只读、编辑、测试优先、搜索/上下文、权限、恢复、长任务和产物交付能力。
- [x] 定义结果分类和证据 artifacts。
- [x] 使用 `npx openspec validate 2026-06-21-supervise-cli-capability-task-matrix --strict` 校验本 change。
- [x] 使用 `npx openspec validate --specs --strict` 校验 canonical specs。

## Harness / 执行器

- [x] Add a deterministic capability-matrix runner command or diagnostics subcommand.
- [x] Add disposable fixture creation for write-capable tasks.
- [x] Add read-only project-scope task mode for analysis tasks.
- [x] Capture `trace.jsonl`, `summary.json`, `diff.patch`, and `classification.json` per run.
- [x] Enforce supervisor no-help boundary: no post-run task workspace edits except cleanup.

- [x] 增加 deterministic capability-matrix runner command 或 diagnostics subcommand。
- [x] 为 write-capable tasks 增加 disposable fixture 创建。
- [x] 为 analysis tasks 增加 read-only project-scope task mode。
- [x] 每次运行记录 `trace.jsonl`、`summary.json`、`diff.patch` 和 `classification.json`。
- [x] 强制 supervisor no-help 边界：运行后不得编辑任务 workspace，清理除外。

## Task Matrix / 任务矩阵

- [x] Implement T01 read-only architecture analysis task.
- [x] Implement T02 focused code edit task.
- [x] Implement T03 test-first bug fix task.
- [x] Implement T04 search and context task.
- [x] Implement T05 permission boundary task.
- [x] Implement T06 tool error recovery task.
- [x] Implement T07 long task decomposition task.
- [x] Implement T08 artifact delivery task.

- [x] 实现 T01 只读架构分析任务。
- [x] 实现 T02 聚焦代码修改任务。
- [x] 实现 T03 测试优先 bug 修复任务。
- [x] 实现 T04 搜索与上下文任务。
- [x] 实现 T05 权限边界任务。
- [x] 实现 T06 工具错误恢复任务。
- [x] 实现 T07 长任务拆分任务。
- [x] 实现 T08 产物交付任务。

## Classification / 归因

- [x] Classify missing or invalid credentials/sandbox/dependencies as `invalid-test-environment`.
- [x] Classify correct tooling with invalid model behavior as `blocked-by-model`.
- [x] Classify absent or unexposed general tools as `blocked-by-cli-capability-gap`.
- [x] Classify routing/projection/preflight/state/feedback/terminal errors as `blocked-by-cli-bug`.
- [x] Add aggregate report grouped by task id, model, profile id, terminal reason, and classification.
- [x] Project reference-style aliases for executable general tools so live failures can distinguish missing tools from model tool-choice errors. Verified with `npx tsx --test src/packages/core-coding-tools/src/index.test.ts` and `npm run typecheck`.
- [x] Surface expanded semantic aliases and engineering-loop guidance in prompt assembly so models see when to use `ApplyPatch`, `Revert`, and `Test` before falling back to shell. Verified with `npx tsx --test src/packages/prompt-assembly/test/prompt-assembly.test.ts`.
- [x] Normalize the same semantic aliases in DeepSeek provider preflight so alias-shaped model calls land on the intended capability ids. Verified with `npx tsx --test src/packages/tool-intent-preflight/test/preflight.test.ts`.
- [x] Add a manifest-driven preflight contract so every model-projected core tool alias normalizes to the visible capability id. Verified with `npx tsx --test src/packages/tool-intent-preflight/test/preflight.test.ts`.
- [x] Add manifest-driven DeepSeek preflight coverage for every model-projected core tool alias and every actual provider-safe core tool function name, so prompt projection and provider intent repair cannot drift as the tool arsenal grows. Verified with `npx tsx --test --test-name-pattern "provider-safe core tool function|manifest-projected" src/packages/tool-intent-preflight/test/preflight.test.ts`.
- [x] Split reference arsenal report evidence into built-in core tools and external adapter tools so CLI capability gaps are not hidden by connector/provider entries. Verified with `npx tsx --test src/packages/core-coding-tools/src/index.test.ts`.
- [x] Verify runtime registration exposes external adapter reference tools as model-visible executable capabilities after `registerRuntimeCoreTools()`. Verified with `npx tsx --test src/packages/runtime/test/runtime-family-capabilities.test.ts`.
- [x] Add a prompt-assembly contract that every actually projected manifest alias appears in tool policy guidance while projection-hidden tools remain excluded. Verified with `npx tsx --test src/packages/prompt-assembly/test/prompt-assembly.test.ts`.
- [x] Add prompt-assembly contracts proving model-facing tool policy guidance names exactly match projected tool schema function names and use the same provider-safe naming shape that DeepSeek preflight repairs back to capability ids. Verified with `npx tsx --test --test-name-pattern "guidance names identical|provider-safe names" src/packages/prompt-assembly/test/prompt-assembly.test.ts`.
- [x] Rename model-facing broad projection from legacy `all` to `safe-all`, keep legacy `all` only as a backward-compatible input, and propagate explicit `toolOptIns` through runtime tool projection so network tools such as `core.web.fetch` are visible only after host opt-in. Verified with `npx tsx --test src/packages/runtime/test/runtime-tool-feedback.test.ts`, `npx tsx --test src/packages/prompt-assembly/test/prompt-assembly.test.ts`, and `npm run typecheck`.
- [x] Expose explicit tool opt-ins through the CLI host adapter with `--tool-opt-in`, keep opt-in flags out of prompt text, and pass the normalized categories into one-shot runtime requests so host-approved network tools project to the model. Verified red/green with `npx tsx --test src/apps/cli/test/cli.test.ts` after adding parser and one-shot projection coverage.
- [x] Apply the same explicit tool projection and `--tool-opt-in` host boundary to scripted chat submissions, so `run` and `chat` expose equivalent model-facing tool surfaces instead of diverging by entrypoint. Verified red/green with `npx tsx --test src/apps/cli/test/cli.test.ts`.
- [x] Update CLI help/usage as a contract surface so `run` and `chat` document `safe-all` and `--tool-opt-in <category>` while no longer recommending legacy `all`. Verified with the CLI help assertions in `npx tsx --test src/apps/cli/test/cli.test.ts`.
- [x] Tighten reference arsenal readiness so reference mappings do not count as executable unless the required capability is a built-in core tool or an explicitly available host/provider/MCP adapter. Verified red/green with `npx tsx --test --test-name-pattern "external adapter-only families" src/packages/core-coding-tools/src/index.test.ts` and full core tool unit coverage with `npx tsx --test src/packages/core-coding-tools/src/index.test.ts`.
- [x] Feed visible capability ids from live tool decision-board traces into capability-matrix profile readiness, so external adapter families pass only when the actual session projected the adapter and otherwise remain actionable CLI capability gaps. Verified red/green with `npx tsx --test --test-name-pattern "visible external adapter capabilities" src/apps/cli/src/diagnostics/capability-matrix.test.ts` and full diagnostic coverage with `npx tsx --test src/apps/cli/src/diagnostics/capability-matrix.test.ts`.
- [x] Read actual session availability from both decision-board `projectionSummaries` and top-level `visibleToolIds`, so simplified or older live traces still prove projected adapter capability availability without bypassing readiness. Verified red/green with `npx tsx --test --test-name-pattern "visibleToolIds" src/apps/cli/src/diagnostics/capability-matrix.test.ts`.
- [x] Remove legacy `all` from capability-matrix task definitions and diagnostics-visible task typing; use `safe-all` for broad projection while leaving lower-level parser/runtime compatibility intact. Verified red/green with `npx tsx --test --test-name-pattern "safe-all instead of legacy all" src/apps/cli/src/diagnostics/capability-matrix.test.ts`.
- [x] Add a CLI dry-run output regression proving the public capability-matrix diagnostics payload serializes `safe-all` and never reintroduces legacy `all` task projections. Verified with `npx tsx --test --test-name-pattern "capability matrix dry-run" src/apps/cli/test/cli.test.ts`.
- [x] Add an explicit `--workspace-root` CLI host boundary and pass the capability-matrix disposable workspace to child runs as an absolute path, so fixture tasks do not rediscover the repository root from parent markers. Verified red/green with `npx tsx --test --test-name-pattern "workspace explicitly|parent paths" src/apps/cli/src/diagnostics/capability-matrix.test.ts`, parser coverage in `npx tsx --test --test-name-pattern "parses the new run and chat commands" src/apps/cli/test/cli.test.ts`, `npm run typecheck`, and live T05 evidence under `.deepseek/capability-matrix/20260625T132156Z/T05/`.
- [x] Prevent read-only permission-boundary prompts from being misclassified as artifact-writing tasks when they mention parent paths such as `../outside-scope.txt`; runtime output-contract inference and prompt assembly now ignore negative mutation instructions and parent traversal artifact paths. Verified red/green with `npx tsx --test --test-name-pattern "read-only parent-path boundary" src/packages/runtime/test/runtime.test.ts`, `npx tsx --test src/packages/prompt-assembly/test/prompt-assembly.test.ts`, `npm run typecheck`, and live T05 evidence under `.deepseek/capability-matrix/20260625T132156Z/T05/`.
- [x] Unify model-visible aliases and DeepSeek preflight normalization for common cross-platform command habits (`cat`, `ls`, `grep`, `rg`, `sed`, `cp`, `mv`, `rm`, `mkdir`, `touch`, `stat`, `env`, `printenv`, `which`, `where`, `bash`, `sh`, `powershell`, `pwsh`, `npm`, `yarn`, `pnpm`, `tar`, `unzip`) so models can discover and call governed semantic tools instead of falling through to unknown-tool or platform-specific shell behavior. Verified red/green with `npx tsx --test --test-name-pattern "command habits|command-habit" src/packages/core-coding-tools/src/index.test.ts src/packages/tool-intent-preflight/test/preflight.test.ts`.
- [x] Extend tool-intent preflight path governance from generic `path` fields to semantic mutation fields such as `sourcePath` and `targetPath`, so copy/move/archive-style tools get the same cross-platform workspace safety and separator normalization before envelope creation. Verified red/green with `npx tsx --test --test-name-pattern "source and target path fields" src/packages/tool-intent-preflight/test/preflight.test.ts` plus full preflight and core tool unit suites.
- [x] Apply the same `sourcePath`/`targetPath` path-field governance to the DeepSeek provider profile itself, so provider-specific alias/argument normalization cannot override the expanded default path-field set and command-habit calls such as `cp`/`mv` stay governed. Verified red/green with `npx tsx --test --test-name-pattern "DeepSeek profile path governance" src/packages/tool-intent-preflight/test/preflight.test.ts` plus full preflight coverage.
- [x] Ensure staged workflow/profile `allowedTools` entries that use tool-family ids expand to the executable capability ids from the canonical tool-family catalog before runtime projection gates are applied, so profile assembly does not hide semantic tools such as `core.text.replace`, `core.file.copy`, or `core.file.move`. Verified red/green with `npx tsx --test --test-name-pattern "family ids" src/packages/runtime/test/runtime-tool-feedback.test.ts` and CLI prompt projection coverage with `npx tsx --test --test-name-pattern "command-habit aliases" src/apps/cli/test/cli.test.ts`.
- [x] Unify runtime workflow-gate progress classification with the projection/tool-family mutation policy, so produce/materialize/repair stages count semantic mutation tools such as `core.text.replace`, file copy/move/delete, directory create, touch, JSON patch, archive, and revert as stage-progress actions instead of rejecting them as `WORKFLOW_REQUIRED_ACTION_MISSED`. Verified red/green with `npx tsx --test --test-name-pattern "semantic mutation tools" src/packages/runtime/test/runtime.test.ts`.
- [x] Expand the CLI host `engineering/coding.v1` profile implement stage from the old write/edit/patch subset to the full governed semantic mutation arsenal, so live engineering runs can actually see the same tools that runtime gate policy accepts. Verified red/green with `npx tsx --test --test-name-pattern "compiles execution-bearing coding prompts" src/apps/cli/src/host/model-selection.test.ts`.
- [x] Tighten capability-matrix artifact contract extraction so slash-separated schema or field names such as `name/version` are not misclassified as required output artifact paths, while real path literals such as `docs/USAGE.md` remain exact-path checked. Verified red/green with `npx tsx --test --test-name-pattern "slash-separated field names" src/apps/cli/src/diagnostics/capability-matrix.test.ts` and full capability-matrix diagnostics coverage.
- [x] Add supervisor-owned task-specific rubric scoring for T07 and bounded untracked-file content evidence in `diff.patch`, so outcome-gate-pass runs can become automated `pass` only when implementation and tests prove the config-loader contract; incomplete rubric evidence stays `partial` with actionable missing evidence. Verified red/green with `npx tsx --test --test-name-pattern "T07|untracked evidence" src/apps/cli/src/diagnostics/capability-matrix.test.ts` and full diagnostics coverage.
- [x] Add supervisor-owned task-specific rubric scoring for T02, T03, and T06, so focused edits, test-first repairs, and tool-error recovery are judged by semantic evidence instead of generic "diff + terminal event" signals alone. T02 now requires `addPositive` RangeError validation and tests, T03 requires slugify regression evidence plus punctuation/edge trimming, and T06 requires initial failure evidence plus a recovered verification command. Verified red/green with `npx tsx --test --test-name-pattern "T02|T03|T06" src/apps/cli/src/diagnostics/capability-matrix.test.ts` and full diagnostics coverage.
- [x] Add supervisor-owned task-specific rubric scoring for T04 and T08, so read-only diagnostics routing and artifact delivery can pass automatically only when trace/diff evidence proves the task contract. T04 now requires read/search tool evidence plus diagnostics routing, command parsing, and rendering evidence; T08 requires exact `docs/USAGE.md` and `examples/config.json` content, package behavior coverage, and verification evidence. Verified red/green with `npx tsx --test --test-name-pattern "T04|T08" src/apps/cli/src/diagnostics/capability-matrix.test.ts`.
- [x] Harden capability-matrix untracked artifact evidence parsing for Markdown artifacts that contain nested fenced code blocks, so supervisor rubrics do not lose verification instructions such as `node --test` when evaluating generated docs. Verified red/green with `npx tsx --test --test-name-pattern "nested markdown fences|T08" src/apps/cli/src/diagnostics/capability-matrix.test.ts`.
- [x] Broaden the T03 supervisor rubric to accept valid punctuation normalization implementations that preserve whitespace and hyphens before cleanup, such as `replace(/[^a-z0-9\s-]/g, '')`, while still requiring regression tests and edge trimming evidence. Verified red/green with `npx tsx --test --test-name-pattern "T03|preserves whitespace and hyphens" src/apps/cli/src/diagnostics/capability-matrix.test.ts`.
- [x] Improve workflow required-action correction guidance for produce/materialize/repair stages so repeated non-progress reads are corrected with an intent-level instruction to call a visible workspace mutation tool, plus the provider-safe function names. Verified red/green with `npx tsx --test --test-name-pattern "produce-stage required-action" src/packages/runtime/test/runtime.test.ts`.
- [x] Stop processing the remaining tool calls from the same model response after a workflow required-action miss correction is queued, so one multi-tool read burst cannot consume the entire correction budget before the model sees corrective guidance. Verified red/green with `npx tsx --test --test-name-pattern "does not exhaust correction budget" src/packages/runtime/test/runtime.test.ts`.
- [x] Add default tool-decision-board next-action guidance even before rejected tool feedback exists, so successful runs still tell the model/evaluator which visible stage tools can advance or verify the workflow instead of reporting `repair-guidance:missing-next-action`. Verified red/green with `npx tsx --test --test-name-pattern "recommends a next action" src/packages/runtime/test/runtime-tool-feedback.test.ts`, full tool-feedback coverage, and live T07 decision quality score `1`.
- [x] Strengthen the generic engineering workflow/profile criteria so bug-fix tasks require regression planning and verification across the observed failure plus boundary, negative, and edge cases when applicable. This addresses the T03 residual gap as engineering workflow guidance rather than a T03-specific benchmark workaround. Verified red/green with `npx tsx --test --test-name-pattern "boundary and negative regression|compiles execution-bearing coding prompts" src/packages/prompt-assembly/test/prompt-assembly.test.ts src/apps/cli/src/host/model-selection.test.ts` and `npx tsx --test src/packages/task-profiles/test/profile-compiler.test.ts`.

- [x] 将凭据/沙箱/依赖缺失或无效分类为 `invalid-test-environment`。
- [x] 将工具链正确但模型行为无效分类为 `blocked-by-model`。
- [x] 将通用工具缺失或未暴露分类为 `blocked-by-cli-capability-gap`。
- [x] 将 routing/projection/preflight/state/feedback/terminal 错误分类为 `blocked-by-cli-bug`。
- [x] 增加按 task id、model、profile id、terminal reason 和 classification 聚合的报告。
- [x] 为可执行通用工具投影参考风格别名，便于 live failure 区分工具缺失与模型选错工具。已用 `npx tsx --test src/packages/core-coding-tools/src/index.test.ts` 和 `npm run typecheck` 验证。
- [x] 在 prompt assembly 中暴露扩展语义别名和工程闭环指导，让模型在退回 shell 前看到何时使用 `ApplyPatch`、`Revert` 和 `Test`。已用 `npx tsx --test src/packages/prompt-assembly/test/prompt-assembly.test.ts` 验证。
- [x] 在 DeepSeek provider preflight 中归一化同一组语义别名，确保 alias 形态的模型调用落到正确 capability id。已用 `npx tsx --test src/packages/tool-intent-preflight/test/preflight.test.ts` 验证。
- [x] 增加基于 manifest 自动枚举的 preflight 契约，确保每个投影给模型的 core tool alias 都能归一化到可见 capability id。已用 `npx tsx --test src/packages/tool-intent-preflight/test/preflight.test.ts` 验证。
- [x] 增加基于 manifest 自动枚举的 DeepSeek preflight 覆盖，验证每个模型可见 core tool alias 和每个真实 provider-safe core tool function name 都能归一化，避免工具库扩展后 prompt 投影与 provider intent repair 漂移。已用 `npx tsx --test --test-name-pattern "provider-safe core tool function|manifest-projected" src/packages/tool-intent-preflight/test/preflight.test.ts` 验证。
- [x] 将 reference arsenal 报告证据拆分为内建 core tools 与外部 adapter tools，避免 connector/provider 条目掩盖 CLI 自身能力缺口。已用 `npx tsx --test src/packages/core-coding-tools/src/index.test.ts` 验证。
- [x] 验证 runtime 注册后 external adapter reference tools 也会作为 model-visible executable capabilities 暴露出来。已用 `npx tsx --test src/packages/runtime/test/runtime-family-capabilities.test.ts` 验证。
- [x] 增加 prompt-assembly 契约，确保实际投影给模型的 manifest alias 都进入 tool policy guidance，同时 projection-hidden tools 仍不被暴露。已用 `npx tsx --test src/packages/prompt-assembly/test/prompt-assembly.test.ts` 验证。
- [x] 增加 prompt-assembly 契约，证明模型可见 tool policy guidance 中的工具名与实际投影的 tool schema function names 完全一致，并使用 DeepSeek preflight 可修复回 capability id 的同一套 provider-safe 命名形态。已用 `npx tsx --test --test-name-pattern "guidance names identical|provider-safe names" src/packages/prompt-assembly/test/prompt-assembly.test.ts` 验证。
- [x] 将模型可见的宽投影命名从 legacy `all` 迁移为 `safe-all`，仅保留 legacy `all` 作为向后兼容输入，并把显式 `toolOptIns` 贯通到 runtime 工具投影，确保 `core.web.fetch` 等网络工具只有 host opt-in 后才可见。已用 `npx tsx --test src/packages/runtime/test/runtime-tool-feedback.test.ts`、`npx tsx --test src/packages/prompt-assembly/test/prompt-assembly.test.ts` 和 `npm run typecheck` 验证。
- [x] 通过 CLI host adapter 暴露显式工具 opt-in：新增 `--tool-opt-in`，确保 opt-in flags 不进入 prompt 文本，并把归一化类别传入 one-shot runtime request，让 host 批准的网络工具可以投影给模型。新增 parser 与 one-shot projection 覆盖后，已用 `npx tsx --test src/apps/cli/test/cli.test.ts` 红/绿验证。
- [x] 将同一套显式工具投影与 `--tool-opt-in` host 边界应用到 scripted chat submissions，确保 `run` 与 `chat` 暴露等价的模型可见工具面，而不是因入口不同而分叉。已用 `npx tsx --test src/apps/cli/test/cli.test.ts` 红/绿验证。
- [x] 将 CLI help/usage 作为契约面同步更新：`run` 与 `chat` 文档展示 `safe-all` 和 `--tool-opt-in <category>`，并不再推荐 legacy `all`。已用 `npx tsx --test src/apps/cli/test/cli.test.ts` 中的 CLI help 断言验证。
- [x] 收紧 reference arsenal readiness：reference mapping 本身不再等同可执行；只有内建 core tool 或 host/provider/MCP 明确可用的 adapter capability 才能让所需 family 通过。已用 `npx tsx --test --test-name-pattern "external adapter-only families" src/packages/core-coding-tools/src/index.test.ts` 红/绿验证，并用 `npx tsx --test src/packages/core-coding-tools/src/index.test.ts` 完整工具单测验证。
- [x] 将 live tool decision-board trace 中的 visible capability ids 输入 capability-matrix profile readiness，确保 external adapter family 只有在本次 session 实际投影了 adapter 时才通过，否则仍归为可行动的 CLI 能力缺口。已用 `npx tsx --test --test-name-pattern "visible external adapter capabilities" src/apps/cli/src/diagnostics/capability-matrix.test.ts` 红/绿验证，并用 `npx tsx --test src/apps/cli/src/diagnostics/capability-matrix.test.ts` 完整诊断测试验证。
- [x] 同时从 decision-board 的 `projectionSummaries` 与顶层 `visibleToolIds` 读取本次 session 可用性，确保简化或旧版 live trace 也能证明 adapter capability 已实际投影，同时不绕过 readiness。已用 `npx tsx --test --test-name-pattern "visibleToolIds" src/apps/cli/src/diagnostics/capability-matrix.test.ts` 红/绿验证。
- [x] 从 capability-matrix 任务定义和诊断可见任务类型中移除 legacy `all`；宽投影统一使用 `safe-all`，同时保留底层 parser/runtime 兼容。已用 `npx tsx --test --test-name-pattern "safe-all instead of legacy all" src/apps/cli/src/diagnostics/capability-matrix.test.ts` 红/绿验证。
- [x] 增加 CLI dry-run 输出回归，证明公开 capability-matrix diagnostics payload 序列化 `safe-all`，不会重新引入 legacy `all` task projection。已用 `npx tsx --test --test-name-pattern "capability matrix dry-run" src/apps/cli/test/cli.test.ts` 验证。
- [x] 增加显式 `--workspace-root` CLI host 边界，并将 capability-matrix 的 disposable workspace 以绝对路径传给 child run，避免 fixture task 因父级 marker 重新发现主仓库根目录。已用 `npx tsx --test --test-name-pattern "workspace explicitly|parent paths" src/apps/cli/src/diagnostics/capability-matrix.test.ts` 红/绿验证、`npx tsx --test --test-name-pattern "parses the new run and chat commands" src/apps/cli/test/cli.test.ts` parser 覆盖、`npm run typecheck`，以及 `.deepseek/capability-matrix/20260625T132156Z/T05/` live T05 evidence 验证。
- [x] 防止只读权限边界 prompt 因提到 `../outside-scope.txt` 等父级路径被误判成 artifact 写入任务；runtime output-contract inference 与 prompt assembly 现在会忽略否定式修改指令和父级遍历 artifact 路径。已用 `npx tsx --test --test-name-pattern "read-only parent-path boundary" src/packages/runtime/test/runtime.test.ts` 红/绿验证、`npx tsx --test src/packages/prompt-assembly/test/prompt-assembly.test.ts`、`npm run typecheck`，以及 `.deepseek/capability-matrix/20260625T132156Z/T05/` live T05 evidence 验证。
- [x] 统一模型可见 alias 与 DeepSeek preflight 归一化，覆盖常见跨平台命令习惯（`cat`、`ls`、`grep`、`rg`、`sed`、`cp`、`mv`、`rm`、`mkdir`、`touch`、`stat`、`env`、`printenv`、`which`、`where`、`bash`、`sh`、`powershell`、`pwsh`、`npm`、`yarn`、`pnpm`、`tar`、`unzip`），让模型能发现并调用受治理的语义工具，而不是落到 unknown tool 或平台特定 shell 行为。已用 `npx tsx --test --test-name-pattern "command habits|command-habit" src/packages/core-coding-tools/src/index.test.ts src/packages/tool-intent-preflight/test/preflight.test.ts` 红/绿验证。
- [x] 将 tool-intent preflight 的路径治理从通用 `path` 字段扩展到 `sourcePath`、`targetPath` 等语义 mutation 字段，确保 copy/move/archive 类工具在创建 envelope 前获得同一套跨平台 workspace 安全和分隔符归一化。已用 `npx tsx --test --test-name-pattern "source and target path fields" src/packages/tool-intent-preflight/test/preflight.test.ts` 红/绿验证，并通过完整 preflight 与 core tool 单测。
- [x] 将同一套 `sourcePath`/`targetPath` 路径字段治理应用到 DeepSeek provider profile 本身，避免 provider-specific alias/argument 归一化覆盖扩展后的默认 path-field 集合，确保 `cp`/`mv` 等命令习惯调用仍受治理。已用 `npx tsx --test --test-name-pattern "DeepSeek profile path governance" src/packages/tool-intent-preflight/test/preflight.test.ts` 红/绿验证，并通过完整 preflight 覆盖。
- [x] 确保 staged workflow/profile 中使用 tool-family id 的 `allowedTools` 会先通过 canonical tool-family catalog 展开为可执行 capability id，再进入 runtime 投影闸门，避免 profile 拼装隐藏 `core.text.replace`、`core.file.copy`、`core.file.move` 等语义工具。已用 `npx tsx --test --test-name-pattern "family ids" src/packages/runtime/test/runtime-tool-feedback.test.ts` 红/绿验证，并用 `npx tsx --test --test-name-pattern "command-habit aliases" src/apps/cli/test/cli.test.ts` 覆盖 CLI prompt 投影。
- [x] 统一 runtime workflow gate 的 progress 分类与投影/工具族 mutation 策略，确保 produce/materialize/repair 阶段把 `core.text.replace`、文件复制/移动/删除、建目录、touch、JSON patch、archive、revert 等语义 mutation 工具视为阶段推进动作，而不是误拒为 `WORKFLOW_REQUIRED_ACTION_MISSED`。已用 `npx tsx --test --test-name-pattern "semantic mutation tools" src/packages/runtime/test/runtime.test.ts` 红/绿验证。
- [x] 将 CLI host 的 `engineering/coding.v1` profile implement 阶段从旧的 write/edit/patch 子集扩展到完整受治理语义 mutation 工具库，确保 live 工程任务实际能看到 runtime gate 已接受的工具。已用 `npx tsx --test --test-name-pattern "compiles execution-bearing coding prompts" src/apps/cli/src/host/model-selection.test.ts` 红/绿验证。
- [x] 收紧 capability-matrix artifact contract 抽取，避免把 `name/version` 这类 slash 分隔的 schema/字段名误判成必须输出的 artifact 路径，同时继续严格校验 `docs/USAGE.md` 等真实路径字面量。已用 `npx tsx --test --test-name-pattern "slash-separated field names" src/apps/cli/src/diagnostics/capability-matrix.test.ts` 红/绿验证，并通过完整 capability-matrix diagnostics 覆盖。
- [x] 增加 supervisor 所有的 T07 任务级 rubric 评分，并在 `diff.patch` 中记录 untracked 文件的有界内容证据，使 outcome gate 已通过的运行只有在实现和测试证明配置加载器契约时才自动变为 `pass`；rubric 证据不足仍保持 `partial` 并输出可行动缺口。已用 `npx tsx --test --test-name-pattern "T07|untracked evidence" src/apps/cli/src/diagnostics/capability-matrix.test.ts` 红/绿验证，并通过完整 diagnostics 覆盖。
- [x] 增加 supervisor 所有的 T02、T03 和 T06 任务级 rubric 评分，使聚焦编辑、测试优先修复和工具错误恢复按语义证据判断，而不是只依赖通用的 “有 diff + 有终态事件” 信号。T02 要求 `addPositive` 的 RangeError 校验与测试，T03 要求 slugify 回归测试以及标点/边界修剪证据，T06 要求先失败证据与恢复后的验证命令成功证据。已用 `npx tsx --test --test-name-pattern "T02|T03|T06" src/apps/cli/src/diagnostics/capability-matrix.test.ts` 红/绿验证，并通过完整 diagnostics 覆盖。
- [x] 增加 supervisor 所有的 T04 和 T08 任务级 rubric 评分，使只读 diagnostics 路由定位和 artifact delivery 只有在 trace/diff 证据证明任务契约时才自动通过。T04 要求 read/search 工具证据，以及 diagnostics routing、command parsing、rendering 证据；T08 要求精确的 `docs/USAGE.md` 与 `examples/config.json` 内容、package 行为覆盖和验证证据。已用 `npx tsx --test --test-name-pattern "T04|T08" src/apps/cli/src/diagnostics/capability-matrix.test.ts` 红/绿验证。
- [x] 加固 capability-matrix 对 untracked artifact 证据的解析，支持包含嵌套 fenced code blocks 的 Markdown artifact，避免 supervisor rubric 在评估生成文档时丢失 `node --test` 等验证说明。已用 `npx tsx --test --test-name-pattern "nested markdown fences|T08" src/apps/cli/src/diagnostics/capability-matrix.test.ts` 红/绿验证。
- [x] 放宽 T03 supervisor rubric 的实现形态识别，接受先保留空白/连字符再清理的合法标点归一化实现，例如 `replace(/[^a-z0-9\s-]/g, '')`，同时仍要求回归测试和边界 trim 证据。已用 `npx tsx --test --test-name-pattern "T03|preserves whitespace and hyphens" src/apps/cli/src/diagnostics/capability-matrix.test.ts` 红/绿验证。
- [x] 改善 produce/materialize/repair 阶段的 workflow required-action 纠偏提示：当模型重复 non-progress read 时，用意图级说明要求调用一个可见 workspace mutation tool，并列出 provider-safe function names。已用 `npx tsx --test --test-name-pattern "produce-stage required-action" src/packages/runtime/test/runtime.test.ts` 红/绿验证。
- [x] 当 workflow required-action miss 已经排入纠偏消息后，停止处理同一个模型响应里的剩余 tool calls，避免一次 multi-tool read burst 在模型看到纠偏前耗尽 correction budget。已用 `npx tsx --test --test-name-pattern "does not exhaust correction budget" src/packages/runtime/test/runtime.test.ts` 红/绿验证。
- [x] 在没有 rejected tool feedback 之前也为 tool decision board 增加默认 next-action guidance，让成功路径仍能告诉模型/评估器哪些当前可见 stage 工具可以推进或验证流程，而不是报告 `repair-guidance:missing-next-action`。已用 `npx tsx --test --test-name-pattern "recommends a next action" src/packages/runtime/test/runtime-tool-feedback.test.ts` 红/绿验证、完整 tool-feedback 覆盖，以及 live T07 decision quality score `1` 验证。
- [x] 强化通用 engineering workflow/profile 标准：bug fix 任务在适用时必须围绕 observed failure，以及 boundary、negative、edge cases 设计回归测试并完成验证。这是对 T03 残余缺口的工程流程修复，不是 T03 专用 benchmark 绕行。已用 `npx tsx --test --test-name-pattern "boundary and negative regression|compiles execution-bearing coding prompts" src/packages/prompt-assembly/test/prompt-assembly.test.ts src/apps/cli/src/host/model-selection.test.ts` 和 `npx tsx --test src/packages/task-profiles/test/profile-compiler.test.ts` 红/绿验证。

## Verification / 验证

- [x] Run matrix dry-run without live model calls.
- [x] Rebuild the CLI before using the default capability-matrix child command so live or dry-run evidence does not execute stale `src/apps/cli/dist/index.js`. Verified with `npm run build:cli`, then `npm exec -- deepseek diagnostics capability-matrix run --task T07 --dry-run --output json`, which serialized `toolProjection:"safe-all"` in both task and child command evidence.
- [x] Run at least T01, T05, and one disposable write-capable task live when credentials are available. Evidence: T01 `.deepseek/capability-matrix/20260625T130522Z/T01/`, T02 `.deepseek/capability-matrix/20260625T130616Z/T02/`, and fixed T05 `.deepseek/capability-matrix/20260625T132156Z/T05/`. T01/T02/T05 produced CLI-only traces with terminal workflow evidence; T05 now has `outcomeGate.status=pass` after workspace-root and read-only output-contract fixes.
- [x] Run T07 long-task decomposition live after semantic mutation/profile/rubric fixes. Evidence: `.deepseek/capability-matrix/20260625T144056Z/T07/` classified `pass`, with `outcomeGate.status=pass`, `classificationCounts.pass=1`, visible semantic mutation arsenal, and no supervisor task-workspace edits.
- [x] Rerun T07 live after decision-board next-action guidance fix. Evidence: `.deepseek/capability-matrix/20260625T144905Z/T07/` classified `pass`, `outcomeGate.status=pass`, `decisionQuality.score=1`, `recommendedNextActionCount=5`, and `gaps=[]`.
- [x] Run T03, T04, T06, and T08 live after tool arsenal/projection fixes. Evidence: `.deepseek/capability-matrix/20260625T155508Z/`. T06 classified `pass` in-run; after T04/T08 rubric and nested artifact parser fixes, replay classification from the recorded trace/diff shows T04, T06, and T08 as `pass`, while T03 remains `partial` because the model omitted slugify punctuation/edge-trim semantic evidence despite complete tool projection and `decisionQuality.score=1`.
- [x] Rerun T03 live after engineering profile criteria were strengthened. Evidence: `.deepseek/capability-matrix/20260625T161540Z/T03/`. The model produced regression tests, punctuation normalization, edge trim, successful verification, and diff evidence with `decisionQuality.score=1`; after the T03 rubric accepted the valid `[^a-z0-9\s-]` implementation shape, replay classification is `pass` with `outcomeGate.status=pass`.
- [x] Live matrix credential gate was satisfied through the CLI credential resolver (`doctor` reported DeepSeek credential source `fake-storage`) even though `DEEPSEEK_API_KEY` and `GLM_API_KEY` shell variables were unset; live provider dispatch succeeded for T01, T02, and T05 without supervisor task-workspace edits.
- [x] Confirm all non-dry smoke evidence is from CLI execution, not supervisor intervention.
- [x] Record findings under `.deepseek/capability-matrix/`.

- [x] 运行 matrix dry-run，不调用 live model。
- [x] 使用默认 capability-matrix child command 前先重建 CLI，避免 live 或 dry-run evidence 执行过期的 `src/apps/cli/dist/index.js`。已用 `npm run build:cli` 后运行 `npm exec -- deepseek diagnostics capability-matrix run --task T07 --dry-run --output json` 验证，task 与 child command evidence 均序列化 `toolProjection:"safe-all"`。
- [x] 凭据可用时至少 live 运行 T01、T05 和一个 disposable write-capable task。证据：T01 `.deepseek/capability-matrix/20260625T130522Z/T01/`、T02 `.deepseek/capability-matrix/20260625T130616Z/T02/`、已修复 T05 `.deepseek/capability-matrix/20260625T132156Z/T05/`。T01/T02/T05 均产生 CLI-only trace 与 terminal workflow evidence；workspace-root 与 read-only output-contract 修复后，T05 的 `outcomeGate.status=pass`。
- [x] 在语义 mutation/profile/rubric 修复后 live 运行 T07 长任务拆分。证据：`.deepseek/capability-matrix/20260625T144056Z/T07/` 分类为 `pass`，`outcomeGate.status=pass`，`classificationCounts.pass=1`，语义 mutation 工具库可见，且 supervisor 未编辑任务 workspace。
- [x] 在 decision-board next-action guidance 修复后再次 live 运行 T07。证据：`.deepseek/capability-matrix/20260625T144905Z/T07/` 分类为 `pass`，`outcomeGate.status=pass`，`decisionQuality.score=1`，`recommendedNextActionCount=5`，且 `gaps=[]`。
- [x] 在工具库/投影修复后 live 运行 T03、T04、T06 和 T08。证据：`.deepseek/capability-matrix/20260625T155508Z/`。T06 在本次运行中分类为 `pass`；补齐 T04/T08 rubric 与嵌套 artifact parser 后，用已记录 trace/diff 重放分类显示 T04、T06、T08 均为 `pass`，T03 仍为 `partial`，原因是模型虽然工具投影完整且 `decisionQuality.score=1`，但缺少 slugify 标点/边界修剪语义证据。
- [x] 在强化 engineering profile 标准后再次 live 运行 T03。证据：`.deepseek/capability-matrix/20260625T161540Z/T03/`。模型产出了回归测试、标点归一化、边界 trim、成功验证和 diff evidence，`decisionQuality.score=1`；T03 rubric 接受合法的 `[^a-z0-9\s-]` 实现形态后，重放分类为 `pass`，`outcomeGate.status=pass`。
- [x] live matrix 凭据门槛已通过 CLI credential resolver 满足（`doctor` 报告 DeepSeek credential source `fake-storage`），即使当前 shell 的 `DEEPSEEK_API_KEY` 和 `GLM_API_KEY` 环境变量未设置；T01、T02、T05 live provider dispatch 均成功，且没有 supervisor 编辑任务 workspace。
- [x] 确认所有 non-dry smoke evidence 都来自 CLI 执行，不来自 supervisor 介入。
- [x] 将 findings 记录到 `.deepseek/capability-matrix/`。
